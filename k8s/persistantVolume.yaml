apiVersion: v1
kind: PersistentVolume
metadata:
  name: mongodb-pv
spec:
  capacity:
    storage: 5Gi
  volumeMode: Filesystem
  hostPath:
    path: /data
  accessModes:
    - ReadWriteOnce
  storageClassName: mongodb-storage
